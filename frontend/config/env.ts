export const ENV = {
  /** defines the route to the backend API */
  API_URL: process.env.API_URL,
  /** defines the route to the backend-for-frontend layer */
  BFF_LAYER_URL: process.env.BFF_LAYER_URL,
  /** defines the access key to the backend API */
  API_KEY: process.env.CLIENT_API_KEY,
  // Hier können später weitere Variablen hin, z.B. ANALYTICS_ID etc.
};